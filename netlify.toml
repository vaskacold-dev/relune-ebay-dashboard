[build]
  publish = "public"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "18"

# Frontend memanggil /api/xxx -> diteruskan ke Netlify Function yang sesuai.
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
